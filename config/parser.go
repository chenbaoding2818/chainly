package config

func (cfg *Config) Parse() error {
	var err error
	// TODO：根据不同的 parser 类型，初始化不同的解析器
	// switch parser {
	// case IniParser:
	// 	cfg.parser = ini.NewParser()
	// case JsonParser:
	// 	cfg.parser = json.NewParser()
	// case YamlParser:
	// 	cfg.parser = yaml.NewParser()
	// }
	return err
}
